<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Mi Portafolio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Si ya tienes Tailwind configurado, se aplicará automáticamente -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-800 font-sans">

    <header class="bg-white shadow-md py-6 mb-10">
        <div class="container mx-auto px-6 flex justify-between items-center">
            <h1 class="text-3xl font-bold text-indigo-600">Mi Portafolio</h1>
            <nav class="space-x-6">
                <a href="#" class="hover:text-indigo-600 transition">Inicio</a>
                <a href="#" class="hover:text-indigo-600 transition">Proyectos</a>
                <a href="#" class="hover:text-indigo-600 transition">Contacto</a>
            </nav>
        </div>
    </header>

    <main class="container mx-auto px-6">
        <section class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-4">¡Hola! 👋 Soy <span class="text-indigo-600">Miguel</span></h2>
            <p class="text-lg text-gray-600">Desarrollador web apasionado por crear experiencias digitales limpias y funcionales.</p>
        </section>

        <section>
            <h3 class="text-2xl font-semibold mb-6 text-indigo-600 text-center">Proyectos Destacados</h3>

            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
                {% for proyecto in proyectos %}
                    <div class="bg-white rounded-2xl shadow-md p-6 hover:shadow-lg transition">
                        <h4 class="text-xl font-bold mb-2 text-gray-800">{{ proyecto.nombre }}</h4>
                        <p class="text-gray-600 mb-4">{{ proyecto.descripcion }}</p>
                        <a href="{{ proyecto.enlace }}" target="_blank" class="text-indigo-600 font-semibold hover:underline">Ver proyecto →</a>
                    </div>
                {% else %}
                    <p class="text-center col-span-3 text-gray-500">Aún no hay proyectos cargados.</p>
                {% endfor %}
            </div>
        </section>
    </main>

    <footer class="mt-16 bg-indigo-600 text-white py-6 text-center">
        <p>© {{ "now"|date("Y") }} Miguel E. Todos los derechos reservados.</p>
    </footer>

</body>
</html>
